{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "g++",
            "args": ["-g", "-o", "${workspaceFolder}/build/main.out", "${workspaceFolder}/main.c"],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
